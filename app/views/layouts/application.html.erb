<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Proser" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%# <%= render :partial => 'layouts/nav_bar' %>
    <div id="site-wrapper">

      <div id="site-canvas">

        <div id="site-menu">
          <div>
                <%= link_to 'Edit profile', edit_user_registration_path %> 
          </div>
          <div>
                <%= link_to "Logout", destroy_user_session_path, method: :delete %>
          </div>
        </div>

          <div data-magellan-expedition="fixed" class="centered top-stick">
            <h1 class="left-off-canvas-toggle main-header noselect" data-transition="ease" ><%= content_for?(:header) ? yield(:header).upcase : "PROSER" %><span class="lines-1 lines">—</span><span class="lines">—</span><span class="lines">—</span></h1>
            <dl class="sub-nav noselect">
              <dd ><%= link_to "Home", root_path, class: ""  %></dd>
              <dd ><%= link_to "Desk", desk_path %></dd>
            </dl>
            <%= render :partial => 'layouts/tool_bar' %>
          </div>
          <%= yield %>
          <%= javascript_include_tag "application" %>


      </div>
    </div>

  </body>
</html>
