<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Proser" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render :partial => 'layouts/nav_bar' %>

    <h1 class="main-header"><%= content_for?(:header) ? yield(:header) : "Proser" %></h1>

    <%= render :partial => 'layouts/tool_bar' %>


    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
